import React from 'react';
import { useEffect } from 'react';

const UseEffectHook = () => {
    // useEffect(()=> {

    // }); //each and every chnage triggers this useEffect
    // useEffect(()=> {

    // },[]);// this will only work once after the page loads
    // useEffect(()=> {

    // },[state]); // this will only trigger whenever the value inside the dependency array changes

  return (
    <div>

    </div>
  )
}

export default UseEffectHook